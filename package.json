{
  "name": "universal-frontend-squeezer",
  "version": "1.0.0",
  "description": "Automatically compresses new/changed images in a PR to keep frontend asset weight down.",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/main.ts -o dist --minify --source-map",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0",
    "sharp": "^0.33.4"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@vercel/ncc": "^0.38.1",
    "typescript": "^5.4.5"
  }
}
